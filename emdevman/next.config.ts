import type { NextConfig } from "next";


const nextConfig: NextConfig = {
  /* config options here */
  images: {
    qualities: [75, 85], // This line is already correct, no change needed
  },
};

export default nextConfig;
